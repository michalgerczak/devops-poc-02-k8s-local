apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-hello-app
  name: my-hello-app
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: my-hello-app
  type: ClusterIP
status:
  loadBalancer: {}
